/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      serverActions: true,
    },
    images: {
      domains: ['spdezhesiktntilymvmb.supabase.co'],
    },
  };
export default nextConfig;
